apiVersion: v1
kind: Service
metadata:
  name: gitops-app
  labels:
    app: gitops-app
spec:
  type: ClusterIP
  selector:
    app: gitops-app
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP